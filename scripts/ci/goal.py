from __future__ import annotations


def optimization_goal() -> str:
    return (
        "protect canonical release safety through one execution contract, "
        "one orchestration path, one observable data flow, and zero drift "
        "between workflows, hooks, shell helpers, and python execution"
    )
