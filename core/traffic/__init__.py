from __future__ import annotations

"""Traffic Autopilot domain (canonical).

This package defines the *domain* primitives for acquiring traffic (ads) without
binding to any particular platform API. Platform specifics live in connectors
(`interfaces/ads/*`) and are accessed via contract-driven plans.
"""
