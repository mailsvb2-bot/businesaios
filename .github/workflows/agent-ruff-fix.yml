name: Agent apply reviewed Ruff fixes

on:
  push:
    branches:
      - agent/client-outcome-idempotency-lease-recovery

permissions:
  contents: write

jobs:
  apply-reviewed-fix:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Apply reviewed patch
        run: python tools/agent_apply_ruff_fix.py
      - name: Commit clean final tree
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'Close strict Ruff debt and reconcile owner locks'
          git push origin HEAD:agent/client-outcome-idempotency-lease-recovery
