name: Apply canonical persistent security migration

on:
  push:
    branches:
      - agent/canonical-catastrophic-safety-repair
    paths:
      - tools/_apply_canonical_persistence_security.py
      - .github/workflows/_apply_canonical_persistence_security.yml
  pull_request:
    branches:
      - main
    paths:
      - tools/_apply_canonical_persistence_security.py
      - .github/workflows/_apply_canonical_persistence_security.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: agent/canonical-catastrophic-safety-repair
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Apply canonical persistent security migration
        env:
          PYTHONDONTWRITEBYTECODE: '1'
        run: python tools/_apply_canonical_persistence_security.py
      - name: Compile and inspect changed sources
        env:
          PYTHONDONTWRITEBYTECODE: '1'
        run: |
          python -m compileall -q \
            security \
            runtime/business_autonomy \
            tests/regression/test_persistent_security_and_sqlite_state.py
          git diff --check
      - name: Commit canonical persistent security migration
        run: |
          rm tools/_apply_canonical_persistence_security.py
          rm .github/workflows/_apply_canonical_persistence_security.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Secure persistent keys and transactional state"
          git push origin HEAD:agent/canonical-catastrophic-safety-repair
