name: Wave 44 apply validated patch

on:
  push:
    branches:
      - agent/coverage-100-wave44-telegram-delivery-canon

permissions:
  contents: write

concurrency:
  group: wave44-telegram-delivery-canon
  cancel-in-progress: false

jobs:
  apply-validated-patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Decode and verify patch
        shell: bash
        run: |
          set -Eeuo pipefail
          python - <<'PY'
          import base64
          import gzip
          from pathlib import Path

          encoded = Path('.wave44.patch.gz.b64').read_text(encoding='utf-8').strip()
          patch = gzip.decompress(base64.b64decode(encoded))
          Path('/tmp/wave44.patch').write_bytes(patch)
          PY
          git apply --check /tmp/wave44.patch

      - name: Apply exact validated files
        shell: bash
        run: |
          set -Eeuo pipefail
          git apply /tmp/wave44.patch
          rm -f .wave44.patch.gz.b64 .github/workflows/wave44-apply.yml
          git add -A -- \
            runtime/_internal/effects_clients/telegram_client.py \
            tests/unit/runtime/test_telegram_client_wave44.py \
            tests/unit/runtime/test_telegram_delivery_helpers_wave44.py \
            .wave44.patch.gz.b64 \
            .github/workflows/wave44-apply.yml
          git config user.name 'BusinesAIOS Coverage Agent'
          git config user.email 'actions@users.noreply.github.com'
          git commit -m 'Coverage 100 wave 44: close Telegram delivery line and branch coverage'
          git push origin HEAD:agent/coverage-100-wave44-telegram-delivery-canon
