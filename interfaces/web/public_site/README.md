# Public Site Surface

This package is the canonical public website surface for BusinesAIOS.

Rules:
- public site must not maintain a second capability catalog;
- capability truth comes from `application.business_autonomy.integration_capability_catalog`;
- roadmap-only capabilities must not be shown as connectable;
- every public claim must be visible from the admin/control-plane surface.
