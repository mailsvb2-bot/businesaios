# 11. Event schema registry for ads/growth events

Define canonical JSON schemas for events: ads_write_attempt/success/fail, ads_guardrails_block, ads_metrics_imported, utm_set@v1. Add schema validation in tests; runtime emits only whitelisted keys.
