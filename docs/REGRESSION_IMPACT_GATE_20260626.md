# Regression impact gate

This document records the first bounded regression impact gate.

The gate maps changed repository paths to required CI checks and blocks generated/runtime artifacts from PR scope.
